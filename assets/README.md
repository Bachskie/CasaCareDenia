Place your provided images here to replace the placeholders used by the site.

Required filenames (case-sensitive):
- hero-photo.jpg  -> the photo displayed in the hero section
- logo.png        -> the logo used in header and favicon

To upload from your machine (example):
mv /path/to/your/logo.png /Users/thomas/Desktop/CasaCareDenia/assets/logo.png
mv /path/to/your/hero.jpg /Users/thomas/Desktop/CasaCareDenia/assets/hero-photo.jpg

Or run the following to preview locally after placing files:

python3 -m http.server 8000
# open http://localhost:8000

If you want me to import the attached images into the workspace, confirm and I will add them as `assets/logo.png` and `assets/hero-photo.jpg` for you.
